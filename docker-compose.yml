services:
  app:
    build:
      context: .
      args:
        # Environment variables needed at build time (baked into Vite bundle)
        VITE_CONVEX_URL: ${VITE_CONVEX_URL}
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
