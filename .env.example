# Convex
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# For E2E tests (set in Convex dashboard for test deployments only)
# CONVEX_IS_TEST=true

# Note: CONVEX_IS_TEST=true enables test-only endpoints:
# - POST /api/test/seed-user - Creates test user and returns auth tokens
# - POST /api/test/cleanup-user - Deletes test user and related data
#
# NEVER set CONVEX_IS_TEST=true on production deployments!
# Use a dedicated Convex preview deployment for E2E testing.
